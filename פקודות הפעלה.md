הנה ריכוז של כל פקודות ההפעלה של **Root OS**, מסודרות לפי סדר הפעולות שאתה צריך:
### 1. התנעה והגדרות (The Bootloader)
**מתי?** פעם אחת בלבד בהתקנה, או כשאתה רוצה לעדכן מפתחות API ורישום פרויקט.
```bash
python root_os/boot.py

```
### 2. סריקת עומק ואינדוקס (The Monster Scanner)
**מתי?** כשאתה רוצה שרות תלמד את כל הפרויקט מחדש ותכתוב מאפס את ה-ROOT.md. זה ה-Reset של הארכיטקטורה.
```bash
python root_os/core/scanner.py

```
### 3. עיכול שינויים מהיר (The Ingestor)
**מתי?** כשעשית שינוי קטן או קומיט ב-Git ואתה רוצה שרות תנתח רק אותו ותעדכן את ה-HISTORY.md וה-ROADMAP.md בלי לסרוק הכל.
```bash
python root_os/core/ingestor.py .

```
### 4. שירות האזנה ברקע (The Watcher Service)
**מתי?** כשאתה רוצה לעבוד והמערכת פשוט תעקוב אחריך ותעדכן הכל אוטומטית בכל פעם שאתה שומר קובץ.
```bash
python root_os/core/root_service.py

```
### 💡 טיפ טכני חשוב (במקרה של שגיאת נתיב)
אם המערכת צועקת שהיא לא מוצאת מודולים (ModuleNotFoundError), תשתמש בקידומת PYTHONPATH כדי להסביר לפייתון איפה השורש של הפרויקט:
 * **לסורק:** PYTHONPATH=. python root_os/core/scanner.py
 * **לאינג'סטור:** PYTHONPATH=. python root_os/core/ingestor.py .
 * **לשירות:** PYTHONPATH=. python root_os/core/root_service.py
### טבלת סיכום מהירה
| פקודה | תפקיד מרכזי | מתי להשתמש? |
|---|---|---|
| **Boot** | הגדרת מפתחות וסביבה | פעם ראשונה / עדכון מפתחות |
| **Scanner** | בניית ארכיטקטורה (ROOT.md) | אחרי שינויי מבנה גדולים |
| **Ingestor** | ניתוח שינויי קוד (Diff) | אחרי קומיט או שינוי לוגי |
| **Service** | אוטומציה מלאה ברקע | בזמן עבודת פיתוח רציפה |
**המפלצת חמושה ומוכנה. איזו מהן אתה מריץ עכשיו?**
